#c shell
#d Prompt with your working directory and history number.

unuse -q `dk_rep 'prompt*'`

if [[ $_dk_op = "use" ]]; then
  setenv oldPS1 "${PS1:-\$ }"
  setenv PS1 '${PWD#$HOME/}:\!$ '
else # unuse
  setenv PS1 "${oldPS1:-\$ }"
  unsetenv oldPS1
fi
